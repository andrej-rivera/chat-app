# Database Structure

Our database will have three main tables

1. User Table (contains all users)

2. Chat Room Table (contains all chat rooms between two users)

3. Chat Message Table (contains all chat messages)
